Put background images here with these names:

EvacuationBackground.png
ShelterBackground.png

The runtime UI will load them automatically. Texture Type can stay default because the runtime converts Texture2D to Sprite when needed.
